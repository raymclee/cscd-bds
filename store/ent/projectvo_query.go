// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"cscd-bds/store/ent/predicate"
	"cscd-bds/store/ent/project"
	"cscd-bds/store/ent/projectvo"
	"cscd-bds/store/ent/schema/xid"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ProjectVOQuery is the builder for querying ProjectVO entities.
type ProjectVOQuery struct {
	config
	ctx         *QueryContext
	order       []projectvo.OrderOption
	inters      []Interceptor
	predicates  []predicate.ProjectVO
	withProject *ProjectQuery
	modifiers   []func(*sql.Selector)
	loadTotal   []func(context.Context, []*ProjectVO) error
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ProjectVOQuery builder.
func (pvq *ProjectVOQuery) Where(ps ...predicate.ProjectVO) *ProjectVOQuery {
	pvq.predicates = append(pvq.predicates, ps...)
	return pvq
}

// Limit the number of records to be returned by this query.
func (pvq *ProjectVOQuery) Limit(limit int) *ProjectVOQuery {
	pvq.ctx.Limit = &limit
	return pvq
}

// Offset to start from.
func (pvq *ProjectVOQuery) Offset(offset int) *ProjectVOQuery {
	pvq.ctx.Offset = &offset
	return pvq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (pvq *ProjectVOQuery) Unique(unique bool) *ProjectVOQuery {
	pvq.ctx.Unique = &unique
	return pvq
}

// Order specifies how the records should be ordered.
func (pvq *ProjectVOQuery) Order(o ...projectvo.OrderOption) *ProjectVOQuery {
	pvq.order = append(pvq.order, o...)
	return pvq
}

// QueryProject chains the current query on the "project" edge.
func (pvq *ProjectVOQuery) QueryProject() *ProjectQuery {
	query := (&ProjectClient{config: pvq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := pvq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := pvq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(projectvo.Table, projectvo.FieldID, selector),
			sqlgraph.To(project.Table, project.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, projectvo.ProjectTable, projectvo.ProjectColumn),
		)
		fromU = sqlgraph.SetNeighbors(pvq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ProjectVO entity from the query.
// Returns a *NotFoundError when no ProjectVO was found.
func (pvq *ProjectVOQuery) First(ctx context.Context) (*ProjectVO, error) {
	nodes, err := pvq.Limit(1).All(setContextOp(ctx, pvq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{projectvo.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (pvq *ProjectVOQuery) FirstX(ctx context.Context) *ProjectVO {
	node, err := pvq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ProjectVO ID from the query.
// Returns a *NotFoundError when no ProjectVO ID was found.
func (pvq *ProjectVOQuery) FirstID(ctx context.Context) (id xid.ID, err error) {
	var ids []xid.ID
	if ids, err = pvq.Limit(1).IDs(setContextOp(ctx, pvq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{projectvo.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (pvq *ProjectVOQuery) FirstIDX(ctx context.Context) xid.ID {
	id, err := pvq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ProjectVO entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ProjectVO entity is found.
// Returns a *NotFoundError when no ProjectVO entities are found.
func (pvq *ProjectVOQuery) Only(ctx context.Context) (*ProjectVO, error) {
	nodes, err := pvq.Limit(2).All(setContextOp(ctx, pvq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{projectvo.Label}
	default:
		return nil, &NotSingularError{projectvo.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (pvq *ProjectVOQuery) OnlyX(ctx context.Context) *ProjectVO {
	node, err := pvq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ProjectVO ID in the query.
// Returns a *NotSingularError when more than one ProjectVO ID is found.
// Returns a *NotFoundError when no entities are found.
func (pvq *ProjectVOQuery) OnlyID(ctx context.Context) (id xid.ID, err error) {
	var ids []xid.ID
	if ids, err = pvq.Limit(2).IDs(setContextOp(ctx, pvq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{projectvo.Label}
	default:
		err = &NotSingularError{projectvo.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (pvq *ProjectVOQuery) OnlyIDX(ctx context.Context) xid.ID {
	id, err := pvq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ProjectVOs.
func (pvq *ProjectVOQuery) All(ctx context.Context) ([]*ProjectVO, error) {
	ctx = setContextOp(ctx, pvq.ctx, ent.OpQueryAll)
	if err := pvq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ProjectVO, *ProjectVOQuery]()
	return withInterceptors[[]*ProjectVO](ctx, pvq, qr, pvq.inters)
}

// AllX is like All, but panics if an error occurs.
func (pvq *ProjectVOQuery) AllX(ctx context.Context) []*ProjectVO {
	nodes, err := pvq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ProjectVO IDs.
func (pvq *ProjectVOQuery) IDs(ctx context.Context) (ids []xid.ID, err error) {
	if pvq.ctx.Unique == nil && pvq.path != nil {
		pvq.Unique(true)
	}
	ctx = setContextOp(ctx, pvq.ctx, ent.OpQueryIDs)
	if err = pvq.Select(projectvo.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (pvq *ProjectVOQuery) IDsX(ctx context.Context) []xid.ID {
	ids, err := pvq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (pvq *ProjectVOQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, pvq.ctx, ent.OpQueryCount)
	if err := pvq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, pvq, querierCount[*ProjectVOQuery](), pvq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (pvq *ProjectVOQuery) CountX(ctx context.Context) int {
	count, err := pvq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (pvq *ProjectVOQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, pvq.ctx, ent.OpQueryExist)
	switch _, err := pvq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (pvq *ProjectVOQuery) ExistX(ctx context.Context) bool {
	exist, err := pvq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ProjectVOQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (pvq *ProjectVOQuery) Clone() *ProjectVOQuery {
	if pvq == nil {
		return nil
	}
	return &ProjectVOQuery{
		config:      pvq.config,
		ctx:         pvq.ctx.Clone(),
		order:       append([]projectvo.OrderOption{}, pvq.order...),
		inters:      append([]Interceptor{}, pvq.inters...),
		predicates:  append([]predicate.ProjectVO{}, pvq.predicates...),
		withProject: pvq.withProject.Clone(),
		// clone intermediate query.
		sql:  pvq.sql.Clone(),
		path: pvq.path,
	}
}

// WithProject tells the query-builder to eager-load the nodes that are connected to
// the "project" edge. The optional arguments are used to configure the query builder of the edge.
func (pvq *ProjectVOQuery) WithProject(opts ...func(*ProjectQuery)) *ProjectVOQuery {
	query := (&ProjectClient{config: pvq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	pvq.withProject = query
	return pvq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ProjectVO.Query().
//		GroupBy(projectvo.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (pvq *ProjectVOQuery) GroupBy(field string, fields ...string) *ProjectVOGroupBy {
	pvq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ProjectVOGroupBy{build: pvq}
	grbuild.flds = &pvq.ctx.Fields
	grbuild.label = projectvo.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.ProjectVO.Query().
//		Select(projectvo.FieldCreatedAt).
//		Scan(ctx, &v)
func (pvq *ProjectVOQuery) Select(fields ...string) *ProjectVOSelect {
	pvq.ctx.Fields = append(pvq.ctx.Fields, fields...)
	sbuild := &ProjectVOSelect{ProjectVOQuery: pvq}
	sbuild.label = projectvo.Label
	sbuild.flds, sbuild.scan = &pvq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ProjectVOSelect configured with the given aggregations.
func (pvq *ProjectVOQuery) Aggregate(fns ...AggregateFunc) *ProjectVOSelect {
	return pvq.Select().Aggregate(fns...)
}

func (pvq *ProjectVOQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range pvq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, pvq); err != nil {
				return err
			}
		}
	}
	for _, f := range pvq.ctx.Fields {
		if !projectvo.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if pvq.path != nil {
		prev, err := pvq.path(ctx)
		if err != nil {
			return err
		}
		pvq.sql = prev
	}
	return nil
}

func (pvq *ProjectVOQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ProjectVO, error) {
	var (
		nodes       = []*ProjectVO{}
		_spec       = pvq.querySpec()
		loadedTypes = [1]bool{
			pvq.withProject != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ProjectVO).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ProjectVO{config: pvq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(pvq.modifiers) > 0 {
		_spec.Modifiers = pvq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, pvq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := pvq.withProject; query != nil {
		if err := pvq.loadProject(ctx, query, nodes, nil,
			func(n *ProjectVO, e *Project) { n.Edges.Project = e }); err != nil {
			return nil, err
		}
	}
	for i := range pvq.loadTotal {
		if err := pvq.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (pvq *ProjectVOQuery) loadProject(ctx context.Context, query *ProjectQuery, nodes []*ProjectVO, init func(*ProjectVO), assign func(*ProjectVO, *Project)) error {
	ids := make([]xid.ID, 0, len(nodes))
	nodeids := make(map[xid.ID][]*ProjectVO)
	for i := range nodes {
		fk := nodes[i].ProjectID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(project.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "project_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (pvq *ProjectVOQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := pvq.querySpec()
	if len(pvq.modifiers) > 0 {
		_spec.Modifiers = pvq.modifiers
	}
	_spec.Node.Columns = pvq.ctx.Fields
	if len(pvq.ctx.Fields) > 0 {
		_spec.Unique = pvq.ctx.Unique != nil && *pvq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, pvq.driver, _spec)
}

func (pvq *ProjectVOQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(projectvo.Table, projectvo.Columns, sqlgraph.NewFieldSpec(projectvo.FieldID, field.TypeString))
	_spec.From = pvq.sql
	if unique := pvq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if pvq.path != nil {
		_spec.Unique = true
	}
	if fields := pvq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, projectvo.FieldID)
		for i := range fields {
			if fields[i] != projectvo.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if pvq.withProject != nil {
			_spec.Node.AddColumnOnce(projectvo.FieldProjectID)
		}
	}
	if ps := pvq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := pvq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := pvq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := pvq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (pvq *ProjectVOQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(pvq.driver.Dialect())
	t1 := builder.Table(projectvo.Table)
	columns := pvq.ctx.Fields
	if len(columns) == 0 {
		columns = projectvo.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if pvq.sql != nil {
		selector = pvq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if pvq.ctx.Unique != nil && *pvq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range pvq.predicates {
		p(selector)
	}
	for _, p := range pvq.order {
		p(selector)
	}
	if offset := pvq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := pvq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ProjectVOGroupBy is the group-by builder for ProjectVO entities.
type ProjectVOGroupBy struct {
	selector
	build *ProjectVOQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (pvgb *ProjectVOGroupBy) Aggregate(fns ...AggregateFunc) *ProjectVOGroupBy {
	pvgb.fns = append(pvgb.fns, fns...)
	return pvgb
}

// Scan applies the selector query and scans the result into the given value.
func (pvgb *ProjectVOGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pvgb.build.ctx, ent.OpQueryGroupBy)
	if err := pvgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ProjectVOQuery, *ProjectVOGroupBy](ctx, pvgb.build, pvgb, pvgb.build.inters, v)
}

func (pvgb *ProjectVOGroupBy) sqlScan(ctx context.Context, root *ProjectVOQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(pvgb.fns))
	for _, fn := range pvgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*pvgb.flds)+len(pvgb.fns))
		for _, f := range *pvgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*pvgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pvgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ProjectVOSelect is the builder for selecting fields of ProjectVO entities.
type ProjectVOSelect struct {
	*ProjectVOQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (pvs *ProjectVOSelect) Aggregate(fns ...AggregateFunc) *ProjectVOSelect {
	pvs.fns = append(pvs.fns, fns...)
	return pvs
}

// Scan applies the selector query and scans the result into the given value.
func (pvs *ProjectVOSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pvs.ctx, ent.OpQuerySelect)
	if err := pvs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ProjectVOQuery, *ProjectVOSelect](ctx, pvs.ProjectVOQuery, pvs, pvs.inters, v)
}

func (pvs *ProjectVOSelect) sqlScan(ctx context.Context, root *ProjectVOQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(pvs.fns))
	for _, fn := range pvs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*pvs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pvs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

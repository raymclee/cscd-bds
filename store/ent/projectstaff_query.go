// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"cscd-bds/store/ent/predicate"
	"cscd-bds/store/ent/project"
	"cscd-bds/store/ent/projectstaff"
	"cscd-bds/store/ent/schema/xid"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ProjectStaffQuery is the builder for querying ProjectStaff entities.
type ProjectStaffQuery struct {
	config
	ctx         *QueryContext
	order       []projectstaff.OrderOption
	inters      []Interceptor
	predicates  []predicate.ProjectStaff
	withProject *ProjectQuery
	loadTotal   []func(context.Context, []*ProjectStaff) error
	modifiers   []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ProjectStaffQuery builder.
func (psq *ProjectStaffQuery) Where(ps ...predicate.ProjectStaff) *ProjectStaffQuery {
	psq.predicates = append(psq.predicates, ps...)
	return psq
}

// Limit the number of records to be returned by this query.
func (psq *ProjectStaffQuery) Limit(limit int) *ProjectStaffQuery {
	psq.ctx.Limit = &limit
	return psq
}

// Offset to start from.
func (psq *ProjectStaffQuery) Offset(offset int) *ProjectStaffQuery {
	psq.ctx.Offset = &offset
	return psq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (psq *ProjectStaffQuery) Unique(unique bool) *ProjectStaffQuery {
	psq.ctx.Unique = &unique
	return psq
}

// Order specifies how the records should be ordered.
func (psq *ProjectStaffQuery) Order(o ...projectstaff.OrderOption) *ProjectStaffQuery {
	psq.order = append(psq.order, o...)
	return psq
}

// QueryProject chains the current query on the "project" edge.
func (psq *ProjectStaffQuery) QueryProject() *ProjectQuery {
	query := (&ProjectClient{config: psq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := psq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := psq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(projectstaff.Table, projectstaff.FieldID, selector),
			sqlgraph.To(project.Table, project.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, projectstaff.ProjectTable, projectstaff.ProjectColumn),
		)
		fromU = sqlgraph.SetNeighbors(psq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ProjectStaff entity from the query.
// Returns a *NotFoundError when no ProjectStaff was found.
func (psq *ProjectStaffQuery) First(ctx context.Context) (*ProjectStaff, error) {
	nodes, err := psq.Limit(1).All(setContextOp(ctx, psq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{projectstaff.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (psq *ProjectStaffQuery) FirstX(ctx context.Context) *ProjectStaff {
	node, err := psq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ProjectStaff ID from the query.
// Returns a *NotFoundError when no ProjectStaff ID was found.
func (psq *ProjectStaffQuery) FirstID(ctx context.Context) (id xid.ID, err error) {
	var ids []xid.ID
	if ids, err = psq.Limit(1).IDs(setContextOp(ctx, psq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{projectstaff.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (psq *ProjectStaffQuery) FirstIDX(ctx context.Context) xid.ID {
	id, err := psq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ProjectStaff entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ProjectStaff entity is found.
// Returns a *NotFoundError when no ProjectStaff entities are found.
func (psq *ProjectStaffQuery) Only(ctx context.Context) (*ProjectStaff, error) {
	nodes, err := psq.Limit(2).All(setContextOp(ctx, psq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{projectstaff.Label}
	default:
		return nil, &NotSingularError{projectstaff.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (psq *ProjectStaffQuery) OnlyX(ctx context.Context) *ProjectStaff {
	node, err := psq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ProjectStaff ID in the query.
// Returns a *NotSingularError when more than one ProjectStaff ID is found.
// Returns a *NotFoundError when no entities are found.
func (psq *ProjectStaffQuery) OnlyID(ctx context.Context) (id xid.ID, err error) {
	var ids []xid.ID
	if ids, err = psq.Limit(2).IDs(setContextOp(ctx, psq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{projectstaff.Label}
	default:
		err = &NotSingularError{projectstaff.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (psq *ProjectStaffQuery) OnlyIDX(ctx context.Context) xid.ID {
	id, err := psq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ProjectStaffs.
func (psq *ProjectStaffQuery) All(ctx context.Context) ([]*ProjectStaff, error) {
	ctx = setContextOp(ctx, psq.ctx, ent.OpQueryAll)
	if err := psq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ProjectStaff, *ProjectStaffQuery]()
	return withInterceptors[[]*ProjectStaff](ctx, psq, qr, psq.inters)
}

// AllX is like All, but panics if an error occurs.
func (psq *ProjectStaffQuery) AllX(ctx context.Context) []*ProjectStaff {
	nodes, err := psq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ProjectStaff IDs.
func (psq *ProjectStaffQuery) IDs(ctx context.Context) (ids []xid.ID, err error) {
	if psq.ctx.Unique == nil && psq.path != nil {
		psq.Unique(true)
	}
	ctx = setContextOp(ctx, psq.ctx, ent.OpQueryIDs)
	if err = psq.Select(projectstaff.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (psq *ProjectStaffQuery) IDsX(ctx context.Context) []xid.ID {
	ids, err := psq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (psq *ProjectStaffQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, psq.ctx, ent.OpQueryCount)
	if err := psq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, psq, querierCount[*ProjectStaffQuery](), psq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (psq *ProjectStaffQuery) CountX(ctx context.Context) int {
	count, err := psq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (psq *ProjectStaffQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, psq.ctx, ent.OpQueryExist)
	switch _, err := psq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (psq *ProjectStaffQuery) ExistX(ctx context.Context) bool {
	exist, err := psq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ProjectStaffQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (psq *ProjectStaffQuery) Clone() *ProjectStaffQuery {
	if psq == nil {
		return nil
	}
	return &ProjectStaffQuery{
		config:      psq.config,
		ctx:         psq.ctx.Clone(),
		order:       append([]projectstaff.OrderOption{}, psq.order...),
		inters:      append([]Interceptor{}, psq.inters...),
		predicates:  append([]predicate.ProjectStaff{}, psq.predicates...),
		withProject: psq.withProject.Clone(),
		// clone intermediate query.
		sql:       psq.sql.Clone(),
		path:      psq.path,
		modifiers: append([]func(*sql.Selector){}, psq.modifiers...),
	}
}

// WithProject tells the query-builder to eager-load the nodes that are connected to
// the "project" edge. The optional arguments are used to configure the query builder of the edge.
func (psq *ProjectStaffQuery) WithProject(opts ...func(*ProjectQuery)) *ProjectStaffQuery {
	query := (&ProjectClient{config: psq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	psq.withProject = query
	return psq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ProjectStaff.Query().
//		GroupBy(projectstaff.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (psq *ProjectStaffQuery) GroupBy(field string, fields ...string) *ProjectStaffGroupBy {
	psq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ProjectStaffGroupBy{build: psq}
	grbuild.flds = &psq.ctx.Fields
	grbuild.label = projectstaff.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.ProjectStaff.Query().
//		Select(projectstaff.FieldCreatedAt).
//		Scan(ctx, &v)
func (psq *ProjectStaffQuery) Select(fields ...string) *ProjectStaffSelect {
	psq.ctx.Fields = append(psq.ctx.Fields, fields...)
	sbuild := &ProjectStaffSelect{ProjectStaffQuery: psq}
	sbuild.label = projectstaff.Label
	sbuild.flds, sbuild.scan = &psq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ProjectStaffSelect configured with the given aggregations.
func (psq *ProjectStaffQuery) Aggregate(fns ...AggregateFunc) *ProjectStaffSelect {
	return psq.Select().Aggregate(fns...)
}

func (psq *ProjectStaffQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range psq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, psq); err != nil {
				return err
			}
		}
	}
	for _, f := range psq.ctx.Fields {
		if !projectstaff.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if psq.path != nil {
		prev, err := psq.path(ctx)
		if err != nil {
			return err
		}
		psq.sql = prev
	}
	return nil
}

func (psq *ProjectStaffQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ProjectStaff, error) {
	var (
		nodes       = []*ProjectStaff{}
		_spec       = psq.querySpec()
		loadedTypes = [1]bool{
			psq.withProject != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ProjectStaff).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ProjectStaff{config: psq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(psq.modifiers) > 0 {
		_spec.Modifiers = psq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, psq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := psq.withProject; query != nil {
		if err := psq.loadProject(ctx, query, nodes, nil,
			func(n *ProjectStaff, e *Project) { n.Edges.Project = e }); err != nil {
			return nil, err
		}
	}
	for i := range psq.loadTotal {
		if err := psq.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (psq *ProjectStaffQuery) loadProject(ctx context.Context, query *ProjectQuery, nodes []*ProjectStaff, init func(*ProjectStaff), assign func(*ProjectStaff, *Project)) error {
	ids := make([]xid.ID, 0, len(nodes))
	nodeids := make(map[xid.ID][]*ProjectStaff)
	for i := range nodes {
		fk := nodes[i].ProjectID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(project.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "project_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (psq *ProjectStaffQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := psq.querySpec()
	if len(psq.modifiers) > 0 {
		_spec.Modifiers = psq.modifiers
	}
	_spec.Node.Columns = psq.ctx.Fields
	if len(psq.ctx.Fields) > 0 {
		_spec.Unique = psq.ctx.Unique != nil && *psq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, psq.driver, _spec)
}

func (psq *ProjectStaffQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(projectstaff.Table, projectstaff.Columns, sqlgraph.NewFieldSpec(projectstaff.FieldID, field.TypeString))
	_spec.From = psq.sql
	if unique := psq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if psq.path != nil {
		_spec.Unique = true
	}
	if fields := psq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, projectstaff.FieldID)
		for i := range fields {
			if fields[i] != projectstaff.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if psq.withProject != nil {
			_spec.Node.AddColumnOnce(projectstaff.FieldProjectID)
		}
	}
	if ps := psq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := psq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := psq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := psq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (psq *ProjectStaffQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(psq.driver.Dialect())
	t1 := builder.Table(projectstaff.Table)
	columns := psq.ctx.Fields
	if len(columns) == 0 {
		columns = projectstaff.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if psq.sql != nil {
		selector = psq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if psq.ctx.Unique != nil && *psq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range psq.modifiers {
		m(selector)
	}
	for _, p := range psq.predicates {
		p(selector)
	}
	for _, p := range psq.order {
		p(selector)
	}
	if offset := psq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := psq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (psq *ProjectStaffQuery) Modify(modifiers ...func(s *sql.Selector)) *ProjectStaffSelect {
	psq.modifiers = append(psq.modifiers, modifiers...)
	return psq.Select()
}

// ProjectStaffGroupBy is the group-by builder for ProjectStaff entities.
type ProjectStaffGroupBy struct {
	selector
	build *ProjectStaffQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (psgb *ProjectStaffGroupBy) Aggregate(fns ...AggregateFunc) *ProjectStaffGroupBy {
	psgb.fns = append(psgb.fns, fns...)
	return psgb
}

// Scan applies the selector query and scans the result into the given value.
func (psgb *ProjectStaffGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, psgb.build.ctx, ent.OpQueryGroupBy)
	if err := psgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ProjectStaffQuery, *ProjectStaffGroupBy](ctx, psgb.build, psgb, psgb.build.inters, v)
}

func (psgb *ProjectStaffGroupBy) sqlScan(ctx context.Context, root *ProjectStaffQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(psgb.fns))
	for _, fn := range psgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*psgb.flds)+len(psgb.fns))
		for _, f := range *psgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*psgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := psgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ProjectStaffSelect is the builder for selecting fields of ProjectStaff entities.
type ProjectStaffSelect struct {
	*ProjectStaffQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (pss *ProjectStaffSelect) Aggregate(fns ...AggregateFunc) *ProjectStaffSelect {
	pss.fns = append(pss.fns, fns...)
	return pss
}

// Scan applies the selector query and scans the result into the given value.
func (pss *ProjectStaffSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pss.ctx, ent.OpQuerySelect)
	if err := pss.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ProjectStaffQuery, *ProjectStaffSelect](ctx, pss.ProjectStaffQuery, pss, pss.inters, v)
}

func (pss *ProjectStaffSelect) sqlScan(ctx context.Context, root *ProjectStaffQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(pss.fns))
	for _, fn := range pss.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*pss.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pss.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (pss *ProjectStaffSelect) Modify(modifiers ...func(s *sql.Selector)) *ProjectStaffSelect {
	pss.modifiers = append(pss.modifiers, modifiers...)
	return pss
}

type Session {
  userId: String!
  name: String!
  username: String!
  email: String!
  avatarUrl: String!
  isAdmin: Boolean!
  isSales: Boolean!
  hasMapAccess: Boolean!
  hasEditAccess: Boolean!
}

extend type Query {
  session: Session!
}

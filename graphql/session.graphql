type Session {
  userId: String!
  name: String!
  username: String!
  email: String!
  avatarUrl: String!
  isAdmin: Boolean!
  isEditor: Boolean!
  hasMapAccess: Boolean!
}

extend type Query {
  session: Session!
}
